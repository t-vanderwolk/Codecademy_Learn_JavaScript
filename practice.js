//function that checks if someone is over 21 and determines if theyre allowed in the bar
let clientsAge = 23;
const checkId = (clientsAge) =>
{
if (clientsAge >= 21 )

console.log('allow in bar.')
}

if (clientsAge < 21) {
return console.log('Allow in bar')
console.log(checkId())
};

